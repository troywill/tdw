#!/usr/bin/env perl
use warnings;
use strict;

system("sudo mount /dev/disk/by-label/thumb_root /mnt/root.thumb/");
system("sudo pacman --sync --refresh");
system("sudo pacman --sync --sysupgrade");
system("sudo killall udevd");
