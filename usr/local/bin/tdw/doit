#!/bin/sh
ts () {
    DAY=`date +%d`;
    DIR="${HOME}/success/2011/10/${DAY}"
    FILENAME="actions-2011-10-${DAY}.txt"
    mkdir --parent ${DIR}
    cd ${DIR}
    emacs ${FILENAME}
}

ts
# tdw-mpr-classical 5
