#!/bin/sh
ts () {
    DAY=`date +%d`;
    DIR="${HOME}/success/2011/11/${DAY}"
    FILENAME="actions-2011-11-${DAY}.txt"
    mkdir --parent ${DIR}
    cd ${DIR}
    emacs ${FILENAME}
}

ts
# tdw-mpr-classical 5
