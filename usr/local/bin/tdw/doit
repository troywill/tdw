#!/bin/sh
ts () {
    DAY=`date +%d`;
    DIR="${HOME}/success/2012/01/${DAY}"
    FILENAME="actions-2012-01-${DAY}.txt"
    mkdir --parent ${DIR}
    cd ${DIR}
    emacs ${FILENAME}
}

ts
# tdw-mpr-classical 5
