#!/usr/bin/perl
use warnings;
use strict;

my @mounts = qw( /mnt/media /dev/disk/by-label/root.thumb );

foreach my $mount ( @mounts ) {
    if ( -e $mount ) {
	print `sudo umount -v $mount`;
    }
}
