#!/usr/bin/perl
use warnings;
use strict;

my @mounts = qw( /mnt/media /dev/disk/by-label/root.thumb /mnt/media-nfs /home/troy/org-nfs /var/cache/pacman/pkg);

foreach my $mount ( @mounts ) {
    if ( -e $mount ) {
	print "==> ";
	print `sudo umount -v $mount`;
    }
}
