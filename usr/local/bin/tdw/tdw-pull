#!/usr/bin/env perl
use warnings;
use strict;

my $stow_dir = "/stow";
chdir $stow_dir or die "Unable to chdir to $stow_dir";
my @dirs = <*>;
foreach my $dir ( @dirs ) {
    print "==> $dir\n";
    chdir $dir or die "Unable to chdir $dir";
    if ( -e '.git' ) {
	system("git pull");
    }
    chdir $stow_dir or die "Unable to chdir to $stow_dir";
}
