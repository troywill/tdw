#!/bin/sh
ts () {
    DAY=`date +%d`;
    DIR="${HOME}/success/2011/09/${DAY}"
    FILENAME="actions-2011-09-${DAY}.txt"
    mkdir --parent ${DIR}
    cd ${DIR}
    emacs ${FILENAME}
}

ts
# tdw-mpr-classical 5
